# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# database
/data/*.db
/data/*.db-journal
/data/*.db-shm
/data/*.db-wal

# backups (real customer/business data — never commit)
/backups/

# contains real staff login PINs — never commit
/docs/TESTING.md
/docs/TESTING.docx

# leftover source zip from the old static-site handover (265MB, superseded
# by app/(public)/ — not project content, shouldn't bloat repo history)
/The-Royal-Chilli-Website-Handover.zip

.vercel
.env*

.vercel
