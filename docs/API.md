# Royal Chilli POS — API Reference

Reference documentation for every route handler under `app/api/**/route.ts` (85 route files, 129 method handlers). Generated by reading each route file's source directly — no guessing on auth/body shapes; anything ambiguous is called out explicitly instead of asserted.

## Conventions

- **Session cookie**: `POST /api/auth/login` verifies `{ name, pin }` against `staff.pin_hash` (bcrypt), then signs a JWT (`jose`, `HS256`, 12h expiry, secret from `JWT_SECRET` env var, fallback dev secret hardcoded in `lib/auth.ts`) containing `{ id, name, role }`. The JWT is set as an **httpOnly, `sameSite=lax`, 12h-maxAge cookie named `pos_session`** (`secure` flag on in production). `POST /api/auth/logout` deletes the cookie. Every other route reads it back via `getSessionFromRequest(req)` (route handlers) or `getSession()` (server components), both in `lib/auth.ts`, which verify the JWT and return `{ id, name, role } | null`.
- **Permission system**: `lib/permissions.ts` exposes six gate functions — `canManageStaff`, `canManageInventory`, `canViewCrm`, `canManageCrm`, `canManageDrivers`, `canManageFinance` — each taking the session's `role` and returning a boolean. Most write (and many read) endpoints call `getSessionFromRequest` then one of these. The role→permission mapping is **configurable at runtime**: it lives in the `role_permissions` table, is cached in-process, refreshed on every edit, and edited via `GET`/`PUT /api/permissions` (surfaced in the Settings UI). If the table can't be reached the code falls back to a hardcoded default mapping baked into `lib/permissions.ts` (fail-closed, not fail-open). A handful of routes instead do a direct role check (e.g. `session.role === "owner" || session.role === "admin"` on `/api/permissions` PUT, or `session.role === "driver"` on `/api/drivers/status`).
- **Error shape**: consistently `{ error: string }` with the HTTP status carrying the meaning — `400` for validation failures, `401` for missing/invalid session or failed permission check, `403` for an authenticated-but-wrong-owner action (e.g. a driver touching someone else's delivery), `404` for missing resources, `409` for a rota scheduling conflict, `500` for unexpected/DB errors. Success responses are `NextResponse.json({ success: true, ... })` (mutations) or a bare data object (GETs), status `200` unless noted (`201` on most creates).
- **Unauthenticated routes**: only `app/api/public/**` (customer-facing ordering/reservation/QR-table-ordering flows, called from the public website with no staff session) and `POST /api/auth/login` (how a session is obtained) are unauthenticated by design. `GET /api/staff` is also intentionally unauthenticated (login-screen name picker; exposes only `id/name/role`). A few other routes were found with **no session check in the code** despite not being under `/api/public` — these are flagged individually below and in the summary at the end; they are not necessarily intentional.

---

## 1. Authentication (`/api/auth/*`)

### `POST /api/auth/login`
**Auth:** none (this is how a session is obtained)
**Body:** `{ name: string, pin: string }`
**Response:** `{ success: true, user: { id, name, role } }`, sets `pos_session` cookie
Looks up `staff` by `name` + `active=1`, compares `pin` against `pin_hash` with bcrypt. `401` on unknown name or bad PIN — same error path is not fully unified (name→"Staff member not found", PIN→"Invalid PIN"), so the response does leak whether a name exists.

### `POST /api/auth/logout`
**Auth:** none required
**Body:** none
**Response:** `{ success: true }`, deletes `pos_session` cookie

### `GET /api/auth/me`
**Auth:** session required (any role)
**Response:** `{ user: { id, name, role } }` or `401 { error: "Unauthorized" }`

---

## 2. Public Website (`/api/public/*`)

All routes in this section are **unauthenticated by design** — they back the customer-facing ordering / reservation / QR-table-ordering flows on the public website, so no session cookie is expected or checked.

### `GET /api/public/delivery-zones/check`
**Auth:** none
**Query:** `postcode` (required)
**Response:** `{ deliverable: false }` or `{ deliverable: true, fee, min_order, zone_name }`
Delegates to `matchDeliveryZone()` in `lib/delivery-zones.ts`.

### `POST /api/public/orders`
**Auth:** none
**Body:** `{ order_type: "takeaway"|"delivery", customer_name, customer_phone, customer_address?, customer_postcode?, notes?, scheduled_for?, items: [{ menu_item_id, quantity, notes?, selected_options? }] }`
**Response:** `201 { success: true, order_number, total, scheduled_for }`
Validates order_type enum, required contact fields, delivery address/postcode when `order_type=delivery`, non-empty cart, and (if present) `scheduled_for` via `validateScheduledTime`. Server resolves each item's price/name and modifiers server-side via `resolveItemWithModifiers` — **client-submitted prices are never trusted**. Delivery orders are matched against a zone and rejected if unreachable or below `min_order`. Auto-creates/matches the customer by phone (`findOrCreateCustomerByPhone`), attaches to the currently open work period if any, and inserts `orders`/`order_items`/`order_item_modifiers`.

### `POST /api/public/reservations`
**Auth:** none
**Body:** `{ customer_name, customer_phone, party_size?, reservation_date, reservation_time, notes?, join_waitlist? }`
**Response:** `{ full: true, message }` (slot full, offers waitlist) or `201 { success: true, id, waitlisted: boolean }`
Capacity check compares reservation count for that date/time against the actual number of `restaurant_tables` rows — not a hardcoded guess. Auto-creates/matches customer by phone.

### `POST /api/public/tables/:tableNumber/order`
**Auth:** none
**Body:** `{ items: [...] }`
**Response:** `{ success: true, order: {id, order_number, status, total}|null, items }`
QR-code "add items to my table's open order" flow; delegates to `lib/dine-in.ts` (`getTableByNumber`, `addItemsToTable`).

### `POST /api/public/tables/:tableNumber/request`
**Auth:** none
**Body:** `{ type: "waiter"|"bill" }`
**Response:** `{ success: true }`
Inserts a row into `table_requests` (status `pending`) — surfaced to staff via `GET /api/table-requests`.

### `GET /api/public/tables/:tableNumber`
**Auth:** none
**Response:** `{ table, order: {id, order_number, status, total}|null, items }`
QR-code table landing page — current table state and open order/items, if any.

---

## 3. Orders / POS (`/api/orders/*`, `/api/kitchen`, `/api/tables`, `/api/table-requests`, `/api/work-periods`)

### `GET /api/orders`
**Auth:** session required (any role)
**Query:** `status` (`"open"` = any non-paid/cancelled order, or an exact status), `date` (YYYY-MM-DD), `table_id`, `order_type`, `source` (`"website"` filters to customer self-service orders with no `staff_id`)
**Response:** `{ orders: [...] }` — table number and staff name flattened in from FK joins

### `POST /api/orders`
**Auth:** session required (any role)
**Body:** `{ order_type, table_id?, customer_name?, customer_phone?, customer_address?, items: [{menu_item_id?, item_name, item_price, quantity, notes?}], notes?, discount?, discount_reason? }`
**Response:** `201 { success: true, order, items }`
Staff-side POS order creation. Computes subtotal/tax (flat 20% VAT)/total server-side from submitted `item_price` — note this endpoint (unlike the public one) does **not** re-resolve prices from the menu table, it trusts the POS client's line items. Attaches to open work period; marks table `occupied` if dine-in.

### `GET /api/orders/:id`
**Auth:** session required (any role)
**Response:** `{ order, items }` or `404`

### `PUT /api/orders/:id`
**Auth:** session required (any role)
**Body:** `{ status?, discount_type?: "percent" | "amount" | null, discount_value?, discount_reason?, notes? }`
**Response:** `{ success: true, order }` or `409` if `discount_type` is sent for an order that's already fully paid.
Updating `status` to `paid`/`cancelled` frees the table. Setting `discount_type`/`discount_value` recomputes `tax`/`discount`/`total` via `lib/order-totals.ts`'s `recalcTotals` (subtotal → VAT → discount → service charge → total) — `discount_value` is a percentage (0-100) when `discount_type` is `"percent"`, or a flat pound amount when `"amount"`. Pass `discount_type: null` to clear it.

### `DELETE /api/orders/:id`
**Auth:** session required (any role)
**Response:** `{ success: true }`
Soft-delete: sets `status="cancelled"` (does not actually delete the row) and frees the table.

### `POST /api/orders/:id/assign-driver`
**Auth:** `canManageDrivers(session.role)`
**Body:** `{ driver_id: number }`
**Response:** `{ success: true, order }`
Validates the target staff member has `role="driver"`. Sets `delivery_status="assigned"`.

### `POST /api/orders/:id/delivery-status`
**Auth:** session required, plus the order's `driver_id` must equal `session.id` (`403` otherwise — a driver can only update their own delivery)
**Body:** `{ status: "out_for_delivery"|"delivered" }`
**Response:** `{ success: true, order }`
Enforces a fixed state machine (`assigned→out_for_delivery→delivered`); any other transition is `400`. Marking `delivered` also sets order `status="paid"` (cash-on-delivery orders are considered settled on delivery).

### `GET /api/orders/:id/items`
**Auth:** **no session check present in the code** (flagged — see summary)
**Response:** `{ items }` with `is_veg` flattened in from the joined `menu_items` row

### `POST /api/orders/:id/items`
**Auth:** session required (any role)
**Body:** `{ items: [{menu_item_id?, item_name, item_price, quantity, notes?}] }`
**Response:** `{ success: true, insertedIds }`
Adds items to an existing order and calls `recalcTotals(id)` afterward (`lib/order-totals.ts`).

### `PUT /api/orders/:id/items`
**Auth:** session required (any role)
**Body:** `{ itemId, action?: "void"|"reduce", quantity?, status? }`
**Response:** `{ success: true }`
`action="void"` cancels one line item and recalcs totals; `action="reduce"` (with `quantity>0`) lowers the line's quantity and recalcs; otherwise a bare `status` update on the item (e.g. kitchen prep status) with no totals recalc.

### `POST /api/orders/:id/payment`
**Auth:** session required (any role)
**Body:** `{ method, amount, tip_amount?, change_given?, reference?, extraOrderIds?: number[] }`
**Response:** `{ success: true, fully_paid: boolean, remaining_balance: number }`
Rejects payment if the order is already `paid`, and rejects `amount` that exceeds the remaining balance (with a small epsilon for rounding). Inserts a `payments` row — a DB trigger updates `orders.amount_paid`. Order flips to `status="paid"` only once `amount_paid >= total`; only then is the table freed and loyalty points awarded (`awardPurchasePoints`). Partial payments are fully supported. `extraOrderIds` lets a single payment settle multiple merged orders at once — each gets its own real `payments` row (not just a status flip) so cash/card reporting totals stay accurate.

### `POST /api/orders/:id/service-charge`
**Auth:** session required (any role)
**Body:** `{ pct: number }` (0–100)
**Response:** `{ success: true, order }` or `409` if the order is already fully paid.
Sets `service_charge_pct` then calls `recalcTotals(id)`.

### `GET /api/kitchen`
**Auth:** **no session check present in the code** (flagged — see summary)
**Response:** `{ orders }` — orders with status `sent_to_kitchen` or `ready`, each with its `items` attached, oldest first

### `PUT /api/kitchen`
**Auth:** **no session check present in the code** (flagged — see summary)
**Body:** `{ orderId, status }`
**Response:** `{ success: true }`
Kitchen display's status-advance action (e.g. mark ready).

### `GET /api/tables`
**Auth:** **no session check present in the code** (flagged — see summary)
**Response:** `{ tables }`

### `POST /api/tables`
**Auth:** **no session check present in the code** (flagged — see summary)
**Body:** `{ table_number, capacity?, location? }`
**Response:** `201 { success: true, table }`

### `PUT /api/tables`
**Auth:** **no session check present in the code** (flagged — see summary)
**Body:** `{ id, status?, capacity?, location?, table_number? }`
**Response:** `{ success: true }`

### `GET /api/table-requests`
**Auth:** session required (any role)
**Response:** `{ requests }` — pending `table_requests` (waiter/bill calls), oldest first

### `PATCH /api/table-requests`
**Auth:** session required (any role)
**Body:** `{ id }`
**Response:** `{ success: true }`
Marks a request `resolved`.

### `GET /api/work-periods`
**Auth:** session required (any role)
**Response:** `{ period, summary: { total_revenue, total_orders, cash_total, card_total, open_orders } }`
Returns the currently open work period (if any) plus today's revenue/payment-split summary.

### `PUT /api/work-periods`
**Auth:** session required (any role)
**Body:** `{ closing_cash?, staff_id? }`
**Response:** `{ period }` or `404` if no period is open
Closes the currently open work period.

### `POST /api/work-periods`
**Auth:** session required (any role)
**Body:** `{ opening_cash?, staff_id? }`
**Response:** `{ period }`
Opens a new work period ("start of shift" till float).

---

## 4. Menu & Modifiers (`/api/menu*`, `/api/modifier-groups/*`)

### `GET /api/menu`
**Auth:** **no session check present in the code** (flagged — see summary; likely intentional since this is the POS/customer menu-display feed, but it lives outside `/api/public`)
**Response:** `{ categories, items }` — active categories/items only, category name/color flattened in

### `GET /api/menu-items`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ items }` — full menu item list (including inactive), for the admin editor

### `POST /api/menu-items`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ category_id, name, price, description?, is_veg?, allergens?, calories?, protein_g?, carbs_g?, fat_g? }` — `category_id`, `name`, `price` required
**Response:** `201 { success: true, item }`

### `PATCH /api/menu-items/:id`
**Auth:** `canManageStaff(session.role)`
**Body:** any subset of `category_id, name, description, price, is_veg, active, display_order, allergens, calories, protein_g, carbs_g, fat_g`
**Response:** `{ success: true, item }`

### `GET /api/menu-items/:id/modifiers`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ attachments: [{group_id, required}] }`

### `POST /api/menu-items/:id/modifiers`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ attachments: [{group_id, required?}] }`
**Response:** `{ success: true }`
Full-replace pattern: deletes all existing `menu_item_modifier_groups` rows for the item then re-inserts the full submitted set (order preserved via `display_order`).

### `GET /api/modifier-groups`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ groups }` — each group with its nested `options[]`

### `POST /api/modifier-groups`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ name, selection_type?: "single"|"multiple", min_select?, max_select?, options: [{name, price_delta?}] }` — name + ≥1 option required
**Response:** `201 { success: true, group }`

### `PATCH /api/modifier-groups/:id`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ name?, selection_type?, min_select?, max_select?, options?: [{name, price_delta?}] }`
**Response:** `{ success: true }`
If `options` is present, full-replaces the option list (delete-then-insert), same pattern as recipe ingredients.

### `DELETE /api/modifier-groups/:id`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ success: true }` (500 with a friendly message if referenced by a past order)

---

## 5. Reservations (`/api/reservations`)

### `GET /api/reservations`
**Auth:** session required (any role)
**Query:** `date` (exact match) or `from` (>= date, "upcoming" view — used by `/pos/reservations`, which lists every booking from today onward with no date picker), `status`. `date` wins if both are given.
**Response:** `{ reservations }`

### `POST /api/reservations`
**Auth:** session required (any role)
**Body:** `{ customer_name, reservation_date, reservation_time, customer_phone?, customer_email?, party_size?, table_id?, notes?, source? }`
**Response:** `201 { success: true, reservation }`
Staff-side booking creation — unlike the public endpoint, does no capacity/waitlist check.

### `PUT /api/reservations`
**Auth:** session required (any role)
**Body:** `{ id, status, table_id?, notes? }` — `status` must be one of `pending|confirmed|seated|cancelled|no_show|waitlisted`
**Response:** `{ success: true, reservation }`
`status="seated"` marks the table `occupied`; `cancelled`/`no_show` frees it.

---

## 6. Delivery Zones & Drivers (`/api/delivery-zones/*`, `/api/drivers/*`)

### `GET /api/delivery-zones`
**Auth:** `canManageDrivers(session.role)`
**Response:** `{ zones }`

### `POST /api/delivery-zones`
**Auth:** `canManageDrivers(session.role)`
**Body:** `{ name, postcode_prefixes: string[], fee?, min_order? }` — name + ≥1 prefix required
**Response:** `201 { success: true, zone }`
Postcode prefixes are normalized (trim + uppercase) before storing.

### `PATCH /api/delivery-zones/:id`
**Auth:** `canManageDrivers(session.role)`
**Body:** any subset of `name, postcode_prefixes, fee, min_order, active`
**Response:** `{ success: true, zone }`

### `DELETE /api/delivery-zones/:id`
**Auth:** `canManageDrivers(session.role)`
**Response:** `{ success: true }` (500 with friendly message if referenced by a past order)

### `GET /api/drivers`
**Auth:** `canManageDrivers(session.role)`
**Response:** `{ drivers }` — active `staff` with `role="driver"`, each enriched with `delivered_count`/`delivered_value` computed from their delivered orders

### `GET /api/drivers/my-deliveries`
**Auth:** session required (any role) — implicitly scoped to `session.id` via the `driver_id` filter, so a non-driver would just get an empty list
**Response:** `{ deliveries }` — the caller's own orders with `delivery_status` in `assigned`/`out_for_delivery`

### `PATCH /api/drivers/status`
**Auth:** session required + `session.role === "driver"` (`403` otherwise)
**Body:** `{ status: "available"|"on_delivery"|"offline" }`
**Response:** `{ success: true }`
Updates the caller's own `staff.driver_status`.

### `GET /api/drivers/unassigned-orders`
**Auth:** `canManageDrivers(session.role)`
**Response:** `{ orders }` — non-cancelled delivery orders with `delivery_status="unassigned"`

---

## 7. Staff & Attendance (`/api/staff`, `/api/employees/*`, `/api/attendance/*`, `/api/availability`, `/api/leave-requests/*`, `/api/rota/*`)

### `GET /api/staff`
**Auth:** none (intentionally — code comment: login-screen name picker needs this before any session exists; only exposes `id, name, role`)
**Response:** `{ staff }` — active staff only

### `POST /api/staff`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ name, pin, role }` — all required
**Response:** `201 { success: true, staff: {id, name, role} }`
Simple staff creation (PIN hashed with bcrypt). Distinct from the richer `/api/employees` create.

### `GET /api/employees`
**Auth:** `canManageStaff(session.role)`
**Query:** `search` (matches name/employee_number/email), `role`, `active` (default `"1"`; pass `"all"` to include inactive)
**Response:** `{ employees }` — full HR profile field set

### `POST /api/employees`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ name, pin, role, email?, phone?, address?, date_of_birth?, hire_date?, employment_type?, pay_rate?, pay_frequency?, emergency_contact_name?, emergency_contact_phone?, notes?, vehicle_type?, vehicle_registration?, driver_status? }`
**Response:** `201 { success: true, employee }`
Enforces an optional `max_employees` cap read from `app_settings` (only if that setting exists). Auto-generates `employee_number` as `RC-EMP-####` from the new row's id. Writes an `audit_logs` entry.

### `GET /api/employees/:id`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ employee }` or `404`

### `PATCH /api/employees/:id`
**Auth:** `canManageStaff(session.role)`
**Body:** any subset of the editable HR fields, plus optional `pin` (rehashed if present)
**Response:** `{ success: true, employee }`
Writes an `audit_logs` entry with the diff.

### `GET /api/attendance`
**Auth:** `canManageStaff(session.role)`
**Query:** `from`, `to`, `staff_id`, `pending_only` (`"1"` = only pending corrections)
**Response:** `{ clockEvents }`
Manager-wide view across all staff clock events. FK to `staff` is explicitly named (`clock_events_staff_id_fkey`) since `clock_events` has two FKs to `staff` (`staff_id` and `approved_by`).

### `POST /api/attendance/:id/correction`
**Auth:** session required — restricted to the event's own owner (`event.staff_id === session.id`, else `404`)
**Body:** `{ requested_clock_in?, requested_clock_out?, reason }` — reason required
**Response:** `{ success: true, clockEvent }`
An employee requesting a fix to a past clock event.

### `PATCH /api/attendance/:id/correction`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ decision: "approved"|"rejected" }`
**Response:** `{ success: true, clockEvent }`
A manager's decision on a pending correction. On approval, applies the requested `clock_in`/`clock_out` values to the actual event (and closes it if `clock_out` was corrected).

### `POST /api/attendance/break`
**Auth:** session required (any role)
**Body:** `{ action: "start"|"end" }`
**Response:** `{ success: true, break }`
Requires an open clock event first (`400` "You're not clocked in"). Prevents double-starting or double-ending a break.

### `POST /api/attendance/clock-in`
**Auth:** session required (any role)
**Response:** `201 { success: true, clockEvent }`
Rejects if already clocked in. Computes lateness against the day's scheduled shift via `findTodaysShiftAndLateness`.

### `POST /api/attendance/clock-out`
**Auth:** session required (any role)
**Response:** `{ success: true, clockEvent }`
Auto-closes any forgotten open break first. Marks the linked shift `completed`.

### `GET /api/attendance/me`
**Auth:** session required (any role)
**Response:** `{ open, openBreak, history }` — caller's own open clock event/break plus last 14 days of history

### `GET /api/availability`
**Auth:** session required; if `staff_id` query param targets someone other than the caller, requires `canManageStaff(session.role)`
**Query:** `staff_id` (defaults to caller)
**Response:** `{ days }` — 7 entries (day_of_week 0–6), defaulted to `is_available=1` where no row exists

### `PUT /api/availability`
**Auth:** session required; if body `staff_id` targets someone other than the caller, requires `canManageStaff(session.role)`
**Body:** `{ staff_id?, days: [{day_of_week, is_available, notes?}] }`
**Response:** `{ success: true }`
Upserts on `(staff_id, day_of_week)`.

### `GET /api/leave-requests`
**Auth:** session required; managers (`canManageStaff`) see everyone's requests, others see only their own
**Response:** `{ leaveRequests }`

### `POST /api/leave-requests`
**Auth:** session required (any role)
**Body:** `{ leave_type, start_date, end_date, reason? }`
**Response:** `201 { success: true, leaveRequest }`
Always created for the caller (`staff_id: session.id`).

### `PATCH /api/leave-requests/:id`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ status: "approved"|"rejected" }`
**Response:** `{ success: true, leaveRequest }`
Writes an `audit_logs` entry.

### `GET /api/rota`
**Auth:** session required; managers see the full week + full staff list, others see only their own shifts (`staff` array empty for non-managers)
**Query:** `week_start` (required)
**Response:** `{ dates, shifts, staff }`

### `POST /api/rota`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ staff_id, shift_date, start_time, end_time, position?, notes?, force? }`
**Response:** `201 { success: true, shift }` or `409 { conflicts }`
Unless `force` is truthy, checks for scheduling conflicts (`checkConflicts` in `lib/rota.ts`) and returns `409` with the conflict list instead of creating the shift.

### `PATCH /api/rota/:id`
**Auth:** `canManageStaff(session.role)`
**Body:** any subset of `start_time, end_time, position, notes, status`
**Response:** `{ success: true, shift }`

### `DELETE /api/rota/:id`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ success: true }` (500 with friendly message if attendance is already recorded against it)

### `POST /api/rota/copy-week`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ from_week_start, to_week_start }`
**Response:** `{ success: true, copied, skipped? }`
Copies all non-cancelled shifts from one week to another, preserving day-of-week offset; silently skips any shift that would collide with an existing one for that staff member/date in the target week.

---

## 8. Payroll (`/api/payroll/*`)

### `GET /api/payroll/entries`
**Auth:** session required; managers see all entries for the period, others see only their own
**Query:** `period_id` (required)
**Response:** `{ entries }`

### `PATCH /api/payroll/entries/:id`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ bonuses?, tips?, deductions?, holiday_pay?, notes? }`
**Response:** `{ success: true, entry }`
Blocks edits once an entry's `status === "paid"`. Recomputes `gross_pay` via `computeGrossPay` and re-derives `status` from `paid_amount` vs the new gross.

### `GET /api/payroll/entries/:id/payments`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ payments }`

### `POST /api/payroll/entries/:id/payments`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ full?: boolean, amount?, method?, notes? }` — `full: true` pays the exact remaining balance, otherwise `amount` must be positive and not exceed the remaining balance
**Response:** `201 { success: true, payment, entry }`
Updates `paid_amount`/`status` on the entry and writes an `audit_logs` entry. Partial payments supported (`status` becomes `partially_paid` until fully covered).

### `GET /api/payroll/periods`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ periods }`

### `POST /api/payroll/periods`
**Auth:** `canManageStaff(session.role)`
**Body:** `{ period_start, period_end }`
**Response:** `201 { success: true, period }` (friendly 500 message if a period with the same dates already exists — a unique-constraint violation)

### `POST /api/payroll/periods/:id/run`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ success: true, entries }`
Computes real hours worked per active employee from attendance data (`computeHoursForPeriod`), pay = `hours × pay_rate` for hourly staff or the flat `pay_rate` for salaried staff, then upserts a `payroll_entries` row per staff member on `(payroll_period_id, staff_id)`. **Idempotent/safe to re-run**: entries already fully `paid` are skipped and reported (`skipped: "already fully paid"`); everything else is recomputed and overwritten in place, preserving any manually-entered `bonuses/tips/deductions/holiday_pay`. Flips the period to `status="processing"` if it was `open`.

---

## 9. Inventory (`/api/ingredients/*`, `/api/suppliers/*`, `/api/purchase-orders/*`, `/api/recipes/*`, `/api/stock-movements`, `/api/inventory/alerts`, `/api/stock-takes/*`, `/api/reports/reconciliation`)

### `GET /api/ingredients`
**Auth:** `canManageInventory(session.role)`
**Query:** `low_stock` (`"1"` filters to `current_stock <= reorder_level`), `search` (name, case-insensitive)
**Response:** `{ ingredients }` — supplier name flattened in

### `POST /api/ingredients`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ name, unit, reorder_level?, reorder_quantity?, cost_per_unit?, supplier_id?, opening_stock? }` — name + unit required
**Response:** `201 { success: true, ingredient }`
If `opening_stock > 0`, immediately records a `stock_movements` row of type `adjustment` — **every stock change, even the very first one, goes through the movements ledger.**

### `PATCH /api/ingredients/:id`
**Auth:** `canManageInventory(session.role)`
**Body:** any subset of `name, unit, reorder_level, reorder_quantity, cost_per_unit, supplier_id, active`
**Response:** `{ success: true, ingredient }`
`current_stock` is deliberately **not** in the editable field list — it can only change via `stock_movements`, so there's always an audit trail for why stock moved.

### `GET /api/suppliers`
**Auth:** `canManageInventory(session.role)`
**Query:** `active` (default `"1"`, pass `"all"` for both)
**Response:** `{ suppliers }`

### `POST /api/suppliers`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ name, contact_name?, phone?, email?, address?, notes? }` — name required
**Response:** `201 { success: true, supplier }`

### `PATCH /api/suppliers/:id`
**Auth:** `canManageInventory(session.role)`
**Body:** any subset of `name, contact_name, phone, email, address, notes, active`
**Response:** `{ success: true, supplier }`

### `GET /api/purchase-orders`
**Auth:** `canManageInventory(session.role)`
**Query:** `status`
**Response:** `{ purchaseOrders }` — supplier name flattened in

### `POST /api/purchase-orders`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ supplier_id, items: [{ingredient_id, quantity, unit_cost}], expected_date?, notes?, status? }` — supplier_id + ≥1 item required
**Response:** `201 { success: true, purchaseOrder }`
Auto-generates order number `PO-YYYYMMDD-###` (sequence per day, from a same-day count query). `status` defaults to `draft` unless explicitly `"ordered"`.

### `GET /api/purchase-orders/:id`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ purchaseOrder, items }` or `404`

### `PATCH /api/purchase-orders/:id`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ status?: "draft"|"ordered"|"cancelled", expected_date?, notes? }`
**Response:** `{ success: true, purchaseOrder }`
(Status transition to `"received"` is not settable here — see the dedicated `receive` endpoint.)

### `POST /api/purchase-orders/:id/receive`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ items?: [{item_id, received_quantity?, expiry_date?}] }`
**Response:** `{ success: true, purchaseOrder }`
Rejects if already `received` or `cancelled`. For each PO line item, records the received quantity (defaulting to the ordered quantity if no override given) and inserts a `stock_movements` row of type `purchase` — **stock changes are audit-tracked the same as everywhere else, never a direct increment.** Also updates each ingredient's `cost_per_unit` to the just-received unit cost (used for recipe costing) and stores per-line `expiry_date`.

### `GET /api/recipes`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ recipes }` — each with computed `recipe_cost` (sum of ingredient qty × cost_per_unit) and `food_cost_pct` (recipe_cost / menu item price)

### `POST /api/recipes`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ name, menu_item_id?, yield_quantity?, yield_unit?, ingredients?: [{ingredient_id, quantity, notes?}] }` — name required
**Response:** `201 { success: true, recipe }`

### `GET /api/recipes/:id`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ recipe, ingredients }` or `404` — recipe includes computed cost/food-cost-% as above

### `PATCH /api/recipes/:id`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ menu_item_id?, name?, yield_quantity?, yield_unit?, notes?, ingredients?: [{ingredient_id, quantity, notes?}] }`
**Response:** `{ success: true }`
If `ingredients` is present, full-replaces the ingredient list (delete-then-insert) — same pattern as modifier group options.

### `GET /api/stock-movements`
**Auth:** `canManageInventory(session.role)`
**Query:** `ingredient_id`, `movement_type`, `from`, `to` (capped at 500 rows, newest first)
**Response:** `{ movements }` — ingredient name/unit and staff name flattened in

### `POST /api/stock-movements`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ ingredient_id, movement_type: "waste"|"adjustment"|"usage", quantity, reason? }`
**Response:** `201 { success: true, movement }`
Rejects `movement_type="purchase"` and anything else outside the three allowed values — purchases must go through PO receiving instead. `waste`/`usage` always store a negative delta (server forces the sign via `-Math.abs(...)`); `adjustment` trusts the client-submitted signed delta as-is.

### `GET /api/inventory/alerts`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ lowStock, expiringSoon }`
`lowStock` = active ingredients where `current_stock <= reorder_level`. `expiringSoon` = PO line items with an `expiry_date` within the next 7 days (and not already past).

### `GET /api/stock-takes`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ stockTakes }` — counted-by staff name flattened in, newest first

### `POST /api/stock-takes`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ location?: "all"|"dry"|"chiller"|"freezer" }` — defaults to `"all"`
**Response:** `201 { success: true, stockTake }`
Opens a take and snapshots every active ingredient's `current_stock` into `stock_take_lines.system_qty` — frozen at this instant so sales during the count don't move the baseline.

### `GET /api/stock-takes/:id`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ stockTake, lines }` — the count sheet; `variance_qty` is a generated column (`counted_qty - system_qty`), live even before posting, `null` while `counted_qty` is unset.

### `PATCH /api/stock-takes/:id/lines`
**Auth:** `canManageInventory(session.role)`
**Body:** `{ lines: [{ingredient_id, counted_qty, reason_code?: "waste"|"spoilage"|"over_portion"|"unknown"|"count_error"}] }`
**Response:** `{ success: true }`
Only while the take is `open` — `400` otherwise.

### `POST /api/stock-takes/:id/post`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ success: true, stockTake }`
For every counted line with a non-zero variance, writes a `stock_movements` row (`movement_type: "adjustment"`, `reference_type: "stock_take"`) so the ledger balance ends up equal to the physical count, and stores `variance_value` (`variance_qty * cost_per_unit`). Locks the take to `status: "posted"`. `400` if not currently `open`. Lines never counted are left alone — no forced variance.

### `GET /api/stock-takes/:id/variance`
**Auth:** `canManageInventory(session.role)`
**Response:** `{ stockTake, lines, totalValue, byReason }` — only non-zero-variance lines; `byReason` sums `variance_value` per `reason_code` (uncategorised lines grouped under `"uncategorised"`). Trending `unknown` value over time is the shrinkage signal.

### `GET /api/reports/reconciliation`
**Auth:** `canManageInventory(session.role)`
**Query:** `from`, `to` (required)
**Response:** `{ period, net_sales, cogs_theoretical, cogs_actual, gp_theoretical, gp_actual, gp_gap, lines }`
Stock-vs-sales, distinct from a stock-take's stock-vs-stock check — only trustworthy once a stock take has posted for the period. Per ingredient: `theoretical_usage` = recipe depletion for everything actually sold in the range (same math as `depleteStockForOrder`); `actual_usage` = everything that left the ledger other than a `purchase`, negated (algebraically equivalent to opening + receipts − closing, without needing to sum the ledger from time zero). `lines` sorted by `|variance_value|` descending — biggest leak first. GP figures are `null` when `net_sales` is 0.

---

## 10. CRM & Loyalty (`/api/customers/*`, `/api/loyalty/*`)

### `GET /api/customers`
**Auth:** `canViewCrm(session.role)`
**Query:** `search` (name/phone, case-insensitive)
**Response:** `{ customers }` — each enriched with `lifetime_spend`, `visit_count` (computed from paid orders), and `tier` (`tierFromSpend` in `lib/crm.ts`)

### `POST /api/customers`
**Auth:** `canViewCrm(session.role)`
**Body:** `{ name, phone, email?, date_of_birth?, address?, referred_by_code? }` — name + phone required
**Response:** `201 { success: true, customer }` (friendly 500 message if phone already exists — unique constraint)
Generates a random `referral_code`. If `referred_by_code` matches an existing customer, awards a referral bonus to both parties via `loyalty_transactions` (+50 to referrer, +25 to the new customer).

### `GET /api/customers/:id`
**Auth:** `canViewCrm(session.role)`
**Response:** `{ customer, orders, transactions }` — customer enriched with `getCustomerStats` (lifetime spend, visit count, favourite dish); last 20 orders and last 20 loyalty transactions

### `PATCH /api/customers/:id`
**Auth:** `canManageCrm(session.role)` (note: stricter than the `GET` on the same resource, which only needs `canViewCrm`)
**Body:** any subset of `name, email, date_of_birth, address, notes`
**Response:** `{ success: true, customer }`

### `GET /api/customers/birthdays`
**Auth:** `canViewCrm(session.role)`
**Response:** `{ upcomingBirthdays }` — customers with a birthday (month/day, year-agnostic) within the next 14 days, sorted soonest-first
All date math done in UTC day-numbers rather than local `Date` getters, specifically to avoid an off-by-one depending on server timezone.

### `POST /api/loyalty/adjust`
**Auth:** `canManageCrm(session.role)`
**Body:** `{ customer_id, points_delta, reason? }`
**Response:** `{ success: true }`
Manual points adjustment (positive or negative), inserted as a `loyalty_transactions` row with `reason="manual_adjustment"`.

### `POST /api/loyalty/redeem`
**Auth:** `canViewCrm(session.role)`
**Body:** `{ customer_id, reward_id }`
**Response:** `{ success: true, points_spent }`
Validates the customer has enough points (`400` otherwise, message states the shortfall). Records the redemption as a negative `loyalty_transactions` delta.

### `GET /api/loyalty/rewards`
**Auth:** `canViewCrm(session.role)`
**Response:** `{ rewards }` — active rewards, cheapest first

### `POST /api/loyalty/rewards`
**Auth:** `canManageCrm(session.role)`
**Body:** `{ name, points_cost, description? }` — name + points_cost required
**Response:** `201 { success: true, reward }`

---

## 11. Finance (`/api/finance/*`, `/api/expenses`, `/api/supplier-payments`)

### `GET /api/finance/cash-reconciliation`
**Auth:** `canManageFinance(session.role)`
**Response:** `{ periods }` — last 20 closed work periods, each with `cash_sales` (sum of cash payments in that period), `expected_cash` (`opening_cash + cash_sales`), `actual_cash` (recorded `closing_cash`), and `variance`

### `GET /api/finance/pnl`
**Auth:** `canManageFinance(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ from, to, revenue, ingredient_purchases, labour_cost, other_expenses, net_profit }`
All four inputs (`getRevenue`, `getIngredientPurchases`, `getLabourCost`, `getOtherExpenses` in `lib/finance.ts`) are fetched in parallel; `net_profit = revenue - ingredient_purchases - labour_cost - other_expenses`.

### `GET /api/finance/vat`
**Auth:** `canManageFinance(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ from, to, vat_rate, revenue, output_vat, vat_applicable_expenses, input_vat, net_vat_due }`
Explicitly documented in-code as an **estimate for the accountant to verify, not HMRC-ready**: assumes sales are standard-rated and raw ingredient purchases are zero-rated (so excluded from input VAT), and only expenses explicitly flagged `vat_applicable` count toward input VAT.

### `GET /api/expenses`
**Auth:** `canManageFinance(session.role)`
**Query:** `from`, `to`
**Response:** `{ expenses }`

### `POST /api/expenses`
**Auth:** `canManageFinance(session.role)`
**Body:** `{ category, description, amount, vat_applicable?, expense_date?, receipt_reference? }` — category/description/amount required
**Response:** `201 { success: true, expense }`
`vat_applicable` defaults to `1` (true) if omitted; `expense_date` defaults to today.

### `GET /api/supplier-payments`
**Auth:** `canManageFinance(session.role)`
**Response:** `{ payments }` — supplier name flattened in

### `POST /api/supplier-payments`
**Auth:** `canManageFinance(session.role)`
**Body:** `{ supplier_id, amount, purchase_order_id?, method?, notes? }` — supplier_id + amount required
**Response:** `201 { success: true, payment }`

---

## 12. Analytics & Reports (`/api/analytics/*`, `/api/reports`, `/api/staff-reports`)

### `GET /api/analytics/inventory`
**Auth:** `canManageStaff(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ waste, total_waste_value, forecast }`
`waste` = ingredients with waste movements in range, valued at current `cost_per_unit`, sorted by value descending. `forecast` = per-ingredient daily consumption rate (waste+usage / days in range) and `days_until_reorder` estimate, sorted soonest-to-reorder first.

### `GET /api/analytics/menu`
**Auth:** `canManageStaff(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ best_sellers, worst_sellers }` — top/bottom 10 items by quantity sold, each with `revenue` and `margin_pct` (computed from linked recipe cost where a recipe exists, else `null`)

### `GET /api/analytics/sales`
**Auth:** `canManageStaff(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ total_revenue, total_orders, avg_order_value, hourly, peak_hour, daily }`
Hourly bucketing uses `getUTCHours()`.

### `GET /api/analytics/staff`
**Auth:** `canManageStaff(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ staff_performance, labour_cost }`
`staff_performance` = active staff with ≥1 order in range, ranked by sales revenue (orders taken as `staff_id` on the order — this measures who rang up the sale, not who cooked/served it).

### `GET /api/reports`
**Auth:** session required (any role) — notably **not** gated by any specific permission function, just requires being logged in
**Query:** `date` (defaults to today)
**Response:** `{ date, summary, byType, topItems, paymentSplit, hourly }`
Single-day end-of-day report: revenue/order-count summary, breakdown by order type, top 10 items, payment method split, hourly breakdown. Excludes cancelled orders.

### `GET /api/staff-reports`
**Auth:** `canManageStaff(session.role)`
**Query:** `from`, `to` (both required)
**Response:** `{ rows, totals }`
Per-staff rollup of hours worked (`computeHoursForPeriod`), late-clock-in count, and labour cost (from `payroll_entries` whose period falls in range) — plus a `totals` row summing hours/cost across all staff.

---

## 13. Settings, Permissions & Audit (`/api/settings`, `/api/permissions`, `/api/audit-logs`)

### `GET /api/settings`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ settings }` — flat `{key: value}` map of the `app_settings` table

### `PUT /api/settings`
**Auth:** `canManageStaff(session.role)`
**Body:** arbitrary `{key: value}` map
**Response:** `{ success: true }`
Upserts every key in the submitted object into `app_settings` on conflict `key`.

### `GET /api/permissions`
**Auth:** `canManageStaff(session.role)`
**Response:** `{ matrix, roles, permissions, labels }`
`matrix` is the full role × permission grid, read fresh from `role_permissions` (bypasses the in-memory cache so the editor UI never shows stale state).

### `PUT /api/permissions`
**Auth:** direct role check — `session.role === "owner" || session.role === "admin"` only (**stricter than `canManageStaff`**, and deliberately not itself configurable through the permission system it edits)
**Body:** `{ role: StaffRole, permission: PermissionKey, granted: boolean }`
**Response:** `{ success: true }`
Validates `role`/`permission` against the known enums. Hardcoded guard: **`role === "owner"` is always rejected** (`400 "Owner permissions cannot be changed"`) so an admin can't accidentally strip every owner's access. Upserts into `role_permissions` on `(role, permission)`, then calls `refreshPermissionsCache()` so the change takes effect immediately for every other endpoint.

### `GET /api/audit-logs`
**Auth:** `canManageStaff(session.role)`
**Query:** `limit` (default 100, capped at 500)
**Response:** `{ logs }` — staff name flattened in (falls back to `"System"` for entries with no `staff_id`)

---

## 14. Misc

### `POST /api/seed` / `GET /api/seed`
**Auth:** none
**Response:** `{ message: "...", skipped: true }`
**Dev-only / effectively decommissioned**: both handlers are no-ops now — they just return a static message pointing at `supabase/schema.sql` to run manually in the Supabase SQL editor. No actual seeding logic remains in this route.

---

## Summary

- **Files enumerated:** 85 `route.ts` files under `app/api/`.
- **Endpoint handlers documented:** 129 (every exported GET/POST/PUT/PATCH/DELETE across those files).

### Routes with no session/auth check — reviewed after this doc was generated

These were flagged by the doc-generation pass as having no `getSessionFromRequest`/`getSession` call despite living outside `app/api/public/**`. Each has since been individually reviewed and either fixed or confirmed intentional:

- **Fixed** — `GET /api/kitchen` and `PUT /api/kitchen` (`app/api/kitchen/route.ts`): now require a session (any authenticated staff role). Previously anyone could view live order/customer data on the kitchen display or change any order's status without logging in.
- **Fixed** — `GET /api/tables`, `POST /api/tables`, `PUT /api/tables` (`app/api/tables/route.ts`): now require a session (any authenticated staff role). Previously table layout/status was world-readable and world-writable.
- **Fixed** — `GET /api/orders/:id/items` (`app/api/orders/[id]/items/route.ts`): now requires a session, matching its `POST`/`PUT` siblings in the same file which already checked it. Previously any order's items/notes were readable by ID with no login.
- **Confirmed intentional, left as-is** — `GET /api/menu` (`app/api/menu/route.ts`): only returns `active=1` categories/items — the same data the public menu page already shows every customer. Not a data leak; no change needed.
- **Confirmed low-risk, left as-is** — `POST /api/seed`, `GET /api/seed`: both handlers are no-ops (see above), so being unauthenticated has no effect.
